style(docs): wrap the self-test note at 100, and fix one more tense slip

The inline issue link's full URL pushed a prose line to 137 characters in
a paragraph that otherwise wraps at 93-99. Reflowed so the link sits at
the start of its own line; nothing enforces the width, so it would not
have self-corrected.

Sweeping the rest of the branch for the same turned up a second line over
100, in meta/scripts/BUILD.bazel, which also still carried the overstated
claim the previous commit removed from the five prose sites: it called
`Action self-test` "a required status check named in repo settings", and
the ruleset does not name it. Now "a check the ruleset is to name".

Refs #312
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
